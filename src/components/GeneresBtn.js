import React from 'react'

const GeneresBtn = ({name}) => {
  return (
    <button className='border border-black rounded p-2 hover:bg-black hover:text-white'>{name}</button>
  )
}

export default GeneresBtn